<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>

    <springProperty name="SLS_ENDPOINT" scope="context" source="aliyun.sls.endpoint"/>

    <springProperty name="SLS_ACCESS_KEY_ID" scope="context" source="aliyun.sls.accessKeyId"/>
    <springProperty name="SLS_ACCESS_KEY_SECRET" scope="context" source="aliyun.sls.accessKeySecret"/>
    <springProperty name="SLS_PROJECT" scope="context" source="aliyun.sls.project"/>
    <springProperty name="SLS_LOG_STORE" scope="context" source="aliyun.sls.logStore"/>


    <conversionRule conversionWord="msg"
                    converterClass="com.qbit.framework.core.api.model.toolkits.logging.SensitiveDataConverter"/>

    <springProperty scope="context" name="applicationName" source="spring.application.name" defaultValue="app"/>

    <property name="LOG_PATTERN"
              value="%clr(%d{${LOG_DATEFORMAT_PATTERN:-yyyy-MM-dd HH:mm:ss.SSS}}){faint} %clr(${LOG_LEVEL_PATTERN:-%5p}) %clr(${PID:- }){magenta} %clr(---){faint} %clr([%15.15t]){faint} %clr([%X{X-TraceId}]){faint} %clr(%-40.40logger{39}){cyan} %clr(:){faint} %msg%n${LOG_EXCEPTION_CONVERSION_WORD:-%wEx}"/>

    <property name="LOG_FILE_PATTERN"
              value="%d{${LOG_DATEFORMAT_PATTERN:-yyyy-MM-dd HH:mm:ss.SSS}} ${LOG_LEVEL_PATTERN:-%5p} ${PID:- } --- [%15.15t] [%X{X-TraceId}] %-40.40logger{39} : %msg%n${LOG_EXCEPTION_CONVERSION_WORD:-%wEx}"/>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>~/logs/${applicationName}.log</file>
        <encoder>
            <pattern>${LOG_FILE_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>logs/${applicationName}-%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <timeBasedFileNamingAndTriggeringPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedFNATP">
                <maxFileSize>100MB</maxFileSize>
            </timeBasedFileNamingAndTriggeringPolicy>
            <maxHistory>30</maxHistory>
        </rollingPolicy>
    </appender>

    <!-- 阿里云 SLS Appender -->
    <appender name="SLS" class="com.qbit.framework.core.api.model.toolkits.logging.SlsAppender">
        <!-- SLS 连接配置 -->
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>INFO</level>
        </filter>
        <accessKeyId>${SLS_ACCESS_KEY_ID}</accessKeyId>
        <accessKeySecret>${SLS_ACCESS_KEY_SECRET}</accessKeySecret>

        <endpoint>${SLS_ENDPOINT}</endpoint>
        <project>${SLS_PROJECT}</project>
        <logstore>${SLS_LOG_STORE}</logstore>

        <!-- 可选配置 -->
        <source>infra-starter</source>
        <topic>application-logs</topic>

        <!-- 性能配置 -->
        <batchSize>100</batchSize>
        <flushInterval>3000</flushInterval>
        <maxRetries>3</maxRetries>

        <!-- 日志格式化（可选） -->
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36}:%line - %msg%n</pattern>
        </encoder>
    </appender>

</configuration>
