framework:
  web:
    exception-advice:
      enabled: true
  feign:
    api:
      base-url: https://circle-test.qbitnetwork.com
      secret: 6YIJXQkhs9mxOQs+74uIIA==
      enabled: true
spring:
  application:
    name: tests
  config:
    import: classpath:/config/application.dynamictp.yml