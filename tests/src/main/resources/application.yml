framework:
  web:
    exception-advice:
      enabled: true
  feign:
    api:
      base-url: https://circle-test.qbitnetwork.com
      secret: 6YIJXQkhs9mxOQs+74uIIA==
      enabled: true
spring:
  profiles:
    active: dev
  application:
    name: tests
  config:
    import:
      - ${spring.profiles.active}/application.dynamictp.yml
      - ${spring.profiles.active}/application.redis.yml